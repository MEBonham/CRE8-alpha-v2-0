import React from 'react';

import ConfigureHeader from './ConfigureHeader';

const Configure = () => {

    return (
        <header>
            <ConfigureHeader />
        </header>
    );
}

export default Configure;